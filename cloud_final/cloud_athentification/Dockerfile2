# Étape 1: Utiliser une image de base pour Maven pour construire l'application
FROM maven:3.8.6-openjdk-17 AS build

# Définir le répertoire de travail
WORKDIR /app

# Copier le fichier pom.xml et télécharger les dépendances
COPY pom.xml .

# Télécharger toutes les dépendances Maven nécessaires
RUN mvn dependency:go-offline

# Copier le code source dans le conteneur
COPY src ./src

# Construire l'application (packager le JAR)
RUN mvn clean package -DskipTests

# Étape 2: Utiliser une image Java 17 pour exécuter l'application
FROM openjdk:17-jdk-slim

# Définir le répertoire de travail
WORKDIR /app

# Copier le fichier JAR généré depuis l'étape de build
COPY --from=build /app/target/crypto_rojo-0.0.1-SNAPSHOT.jar /app/crypto_rojo.jar

# Exposer le port sur lequel l'application Spring Boot va écouter
EXPOSE 7070

# Commande pour exécuter l'application Spring Boot
ENTRYPOINT ["java", "-jar", "cryptos.jar"]
